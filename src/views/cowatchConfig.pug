extends layout

block scripts
  script(src="/public/utils.js")
  script(src="/src/scripts/cowatchConfig.js")

block content
    div(style='text-align:center')
        h1 Platform Extensibility App
        p Welcome to #{title}
        div
            label(for="tabChoice")
            | Click Save below to set up the tab