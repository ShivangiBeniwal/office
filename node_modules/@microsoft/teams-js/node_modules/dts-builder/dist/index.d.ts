import { Bundle } from "./types";
/** Global options */
export declare const DTSBuilderOptions: {
    verbose: boolean;
};
/**
 * Receives a list of Bundles and generates the d.ts file according
 * to bundle configuration.
 */
export declare function generateBundles(bundles: Array<Bundle>): Promise<Array<string>>;
